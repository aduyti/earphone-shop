import React from 'react';
import './Cart.css'

const Cart = ({ products }) => {

    return (
        <div className="cart-container">
            hello
        </div>
    );
};

export default Cart;